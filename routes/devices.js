const express = require('express'); const router = express.Router(); const { v4: uuidv4 } = require('uuid'); const { Device } = require('../models'); const { authenticate } = require('../middleware/auth'); router.get('/', authenticate, async (req, res) => { try { const devices = await Device.find({ owner: req.userId }); res.json({ devices }); } catch(err) { res.status(500).json({ error: err.message }); } }); router.post('/register', authenticate, async (req, res) => { try { const { name, platform } = req.body; const deviceId = uuidv4(); const device = await Device.create({ name, deviceId, platform, owner: req.userId, isOnline: true, lastSeen: new Date() }); res.status(201).json({ deviceId: device.deviceId, device }); } catch(err) { res.status(500).json({ error: err.message }); } }); module.exports = router;
